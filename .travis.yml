sudo: required
language: bash
services:
  - docker
before_script:
  - docker build --tag ${KBC_APP_REPOSITORY} .
deploy:
  provider: script
  skip_cleanup: true
  script: ./deploy.sh
  on:
    tags: true  
